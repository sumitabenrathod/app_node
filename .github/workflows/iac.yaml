name: Terraform Plan

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  terraform-plan:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: iac

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Terraform
        run: |
          sudo apt-get update -y
          sudo apt-get install -y wget unzip
          wget https://releases.hashicorp.com/terraform/1.6.6/terraform_1.6.6_linux_amd64.zip
          unzip terraform_1.6.6_linux_amd64.zip
          sudo mv terraform /usr/local/bin/
          terraform version
      - name: Set AWS credentials
        run: |
          echo "AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}" >> $GITHUB_ENV
          echo "AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}" >> $GITHUB_ENV
          echo "AWS_REGION=${{ secrets.AWS_REGION }}" >> $GITHUB_ENV
      - name: Terraform Init
        run: |
          ls -la          
          terraform init -backend-config=envs/dev/backend.tf
          terraform plan -var-file=envs/dev/dev.tfvars
          terraform apply -auto-approve -var-file=envs/dev/dev.tfvars
                    
      - name: Terraform Validate
        run: terraform validate

      # - name: Terraform Plan for dev envionments
      #   run: |
      #     ls -la
      #     terraform plan -var-file=envs/dev/dev.tfvars
      #     terraform apply -auto-approve -var-file=envs/dev/dev.tfvars
      #     # terraform destroy -auto-approve -var-file=envs/dev.tfvars

      # - name: Terraform Plan for prod envionments
      #   run: |
      #     terraform plan -var-file=envs/prod/prod.tfvars
      #     terraform apply -auto-approve -var-file=envs/prod/prod.tfvars
